{
  "name": "test-repo-2",
  "private": true,
  "scripts": {
    "dev": "npx sass scss:css --watch & npx live-server"
  },
  "devDependencies": {
    "sass": "^1.70.0",
    "live-server": "^1.2.2"
  },
  "scripts": {
  "sass": "npx sass scss:css --watch",
  "serve": "npx live-server",
  "dev": "npx npm-run-all --parallel sass serve"
}

}
